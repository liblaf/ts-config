# This file is @generated by <https://github.com/liblaf/copier-shared>.
# DO NOT EDIT!

name: Shared / Approve

on:
  schedule:
    - cron: "0 0 * * 0" # Runs at 00:00, only on Sunday
  workflow_dispatch:

env:
  FORCE_COLOR: 1

jobs:
  approve:
    name: Approve
    runs-on: ubuntu-latest
    environment:
      name: approve
      deployment: false
    steps:
      - id: auth
        name: Auth
        uses: liblaf/actions/auth@30c85b7c83d5db558c3113c5a7297837f6fe4904 # v1
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}
      - name: Approve
        uses: liblaf/actions/approve@30c85b7c83d5db558c3113c5a7297837f6fe4904 # v1
        with:
          approve-token: ${{ steps.auth.outputs.token }}
          label: ${{ matrix.label }}
    strategy:
      matrix:
        label:
          - copier
          - release-please
      fail-fast: false
